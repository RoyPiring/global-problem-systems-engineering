lens: global-problem-systems-engineering
featured_enabled: auto
threshold: 1
scored_at: '2026-07-20'
human_pin: []
systems:
- slug: clinical-ai-gateway
  axis_scores:
    technical_depth: 9
    voice: 9
    validation: 9
  axis_reasoning:
    technical_depth: Eight phases over a real routing topology (LiteLLM router, Ollama
      local floor, Presidio redaction, cross-border region check, team budget fallback),
      and the diagram names the actual failure edges including `unreachable:9999`,
      one fallback hop, and budget exhaustion routing to local inference.
    voice: First person and concrete throughout, and it states its own limit plainly
      in `The route-back behavior requires valid cloud credentials`, which is the
      opposite of filler.
    validation: 'The offline-floor phase carries the strongest evidence: response
      headers `x-litellm-model-api-base: http://ollama:11434` and `x-litellm-attempted-fallbacks:
      1` with HTTP 200 under a forced connectivity cut, backed by five-bar drills
      and a Grafana per-capita budget gauge.'
  average: 9.0
  rationale: Offline floor proven by response headers under a forced connectivity
    cut
  featured: true
- slug: cryptographic-supply-chain-tracker
  axis_scores:
    technical_depth: 8
    voice: 5
    validation: 6
  axis_reasoning:
    technical_depth: 'Real cryptographic design across eleven phases: KMS-backed ECDSA
      over canonical JSON, a DynamoDB ledger keyed by `chain_id` plus `ledger_key`,
      an independent anomaly path, and a public audit API that separates HTTP 400
      from HTTP 422 for integrity failures.'
    voice: Third-person and repetitive, leaning on the same reframing sentence several
      times (`transforms the platform from a basic event tracker into a cryptographically
      verifiable ledger`), with no author present in the prose.
    validation: The crisis-scenario phase is strongest with a concrete HTTP 400 on
      the pharma skip-shipper run and a 14-item checklist, but signature verification
      tests are explicitly partial and KMS end-to-end integration tests are deferred
      to future work.
  average: 6.3
  rationale: KMS-signed custody chain with an ordering gate that rejects a skipped
    handoff
  featured: true
- slug: national-edtech-platform
  axis_scores:
    technical_depth: 6
    voice: 5
    validation: 5
  axis_reasoning:
    technical_depth: Fifteen containers wired through NGINX with Keycloak OIDC federation
      and a Prometheus/Grafana/cAdvisor path is a working integration, but the diagram
      is a generic service map with no failure edges, stray blank lines, and an `Assessment
      & Sessions` subgraph that actually holds Jicofo/JVB/XMPP.
    voice: Passive and template-shaped (`Step goals and approach` repeated nine times),
      with the author absent and sentences like `The platform is documented as a production-style
      system` carrying little information.
    validation: 'The caching phase is the most honest evidence, reporting repeated
      `X-Cache-Status: MISS` and admitting the rules need tuning, and the DIKSHA evaluation
      names gaps in logging, tracing, and alerting, but no phase carries measured
      numbers or a benchmark figure.'
  average: 5.3
  rationale: Fifteen-container stack with a live metrics pipeline and an honest cache-miss
    finding
  featured: true
- slug: realtime-scam-detection-api
  axis_scores:
    technical_depth: 8
    voice: 9
    validation: 9
  axis_reasoning:
    technical_depth: Fast-path feed lookup plus a swappable `DetectionModel.score`
      gateway is a clean seam for a later ML backend, and the ADR reasons from unit
      cost (2M free GCP invocations versus API Gateway request fees), though the deployed
      system is a single function rather than a multi-service stack.
    voice: Direct and unusually candid, with a whole section titled around what the
      system cannot do, naming user education, bank fraud controls, and law enforcement
      as things it does not replace.
    validation: 'The GCP deployment phase has the strongest evidence: measured against
      live `.run.app` with precision 1.0, recall 0.6, F1 0.75 on 9 TP / 0 FP / 6 FN,
      p95 137.62 ms over 10 requests, and the failed recall SLO is reported rather
      than buried.'
  average: 8.7
  rationale: Live SLO validation that reports the recall miss instead of hiding it
  featured: true
